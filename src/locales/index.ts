import editorialEn from "./editorial.en.json";
import { en } from "./en";
// Add a fully reviewed Arabic dictionary here before enabling Arabic in config.
// Set lang="ar" and dir="rtl" together. No unapproved translation is published.
export const locale = en;

export const editorial = editorialEn;
